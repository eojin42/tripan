<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripan.app.mapper.TripMemberMapper">

    <select id="findByTripIdAndMemberId" resultType="com.tripan.app.trip.domain.entity.TripMember">
       	SELECT 
            trip_member_id,
            trip_id,
            member_id,
            role,
            joined_at,
            invitation_status
        FROM trip_member
        WHERE trip_id = #{tripId} 
          AND member_id = #{memberId}
    </select>
</mapper>